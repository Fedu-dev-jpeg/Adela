import React from "react";
import ReactDOM from "react-dom/client";
import LitCafeApp from "./LitCafeApp.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <LitCafeApp />
  </React.StrictMode>,
);
